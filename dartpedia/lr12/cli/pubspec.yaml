name: cli
description: A sample command-line application.
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.8.1

dependencies:
  http: ^1.3.0
  logging: ^1.2.0
  command_runner:
    path: ../../lr8/command_runner   # ← ИЗМЕНИТЬ НА СУЩЕСТВУЮЩИЙ ПУТЬ
  wikipedia:
    path: ../wikipedia

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8